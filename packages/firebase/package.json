{
  "name": "@rov/firebase",
  "version": "1.0.0",
  "description": "Firebase configuration and Cloud Functions for Realm of Valor",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:rules": "firebase deploy --only firestore:rules"
  },
  "dependencies": {
    "@rov/types": "workspace:*",
    "@rov/logic": "workspace:*",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.2.0"
  }
}